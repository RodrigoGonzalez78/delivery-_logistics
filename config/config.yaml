data:
  raw_path: "data/raw/data.csv"
  target_col: "delivery_time_hours"

training:
  test_size: 0.2
  random_state: 42
  batch_size: 32
  epochs: 30
  learning_rate: 0.001

model:
  save_path: "models/delivery_model.keras"
  preprocessor_path: "models/preprocessor.pkl"