data:
  raw_path: "data/raw/delivery_dataset.csv" # Cambia esto por el nombre real
  target_col: "delivery_time_hours"

training:
  test_size: 0.2
  random_state: 42
  batch_size: 32
  epochs: 50
  learning_rate: 0.001

model:
  save_path: "models/delivery_model.keras"
  preprocessor_path: "models/preprocessor.pkl"